# do: `crontab -e` and paste these lines in. you may need to adjust the paths.

# Every hour
0 * * * *  cd /srv/www/argos/; /srv/www/argos/env/bin/python -c 'import membrane.collector as c; c.collect()'